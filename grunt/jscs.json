{
  "options": {
    "config": ".jscsrc",
    "esnext": true,
    "verbose": true
  },

  "status": {
    "src": ["<%= paths.project %>"],
    "options": {
      "force": true
    }
  },

  "write": {
    "src": ["<%= paths.project %>"],
    "options": {
      "fix": true
    }
  }
}
